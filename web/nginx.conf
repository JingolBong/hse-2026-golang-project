server {
    listen 80;
    server_name _;

    root /usr/share/nginx/html;
    index index.html;

    # SPA: let Angular's client-side router handle unknown paths
    # (e.g. /myprojects, /project-stat) instead of returning 404.
    location / {
        try_files $uri $uri/ /index.html;
    }
}
